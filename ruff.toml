line-length = 120
target-version = "py312"

[format]
docstring-code-format = true
docstring-code-line-length = 80
quote-style = "double"
line-ending = "lf"

[lint]
preview = true
select = ["ALL"]
ignore = [
    # Only permits documenting exceptions raised by an explicit `raise` in the
    # same function, which forbids documenting ones a caller will genuinely see
    # propagated from a callee or from a returned decorator.
    "DOC502", # raised exception is not explicitly raised
]

[lint.per-file-ignores]
"noxfile.py" = [
    "D", "DOC" # docstrings
]
"**/__init__.py" = [
    "F401", # imported but unused
    "F403", # unable to detect undefined names
]
"tests/**" = [
    "D",       # docstrings
    "DOC",     # docstrings
    "S101",    # use of assert
    "PLR2004", # magic value comparison
]
"testbot/**" = [
    "ALL"
]

[lint.isort]
required-imports = ["from __future__ import annotations"]
force-single-line = true

[lint.pydocstyle]
convention = "numpy"
